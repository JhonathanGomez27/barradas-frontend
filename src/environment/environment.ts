const host = 'http://localhost:3001';
// const host = 'https://www.confiabarradas.com';

export const environment = {
    host: `${host}`,
    url: `${host}/api`,
    hostComplete: `${host}/complete`,
    pagination: 20,
    production: false,
}
